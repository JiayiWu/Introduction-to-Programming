8.如果没有读到文件时，会抛出找不到文件的异常，导致程序停止运行，此时用try-catch可以将错误回收，并继续执行后续的程序，不会导致程序终止。

9.第一种可以但第二种不可以，java可以根据参数个数选择合适的方法来执行，但若名字相同，参数完全相同，则无法判断应该调用哪一个方法。

10.调用的是Son的run方法,若在子类中重写了某一方法，则按照子类中的方法来执行，而不采用父类的方法。

11.构造新的对象时，首先会先调用父类的构造器，并且是没有参数的，再调用子类的构造器，按照当前输入的参数来判断调用哪种构造器
我的试验方法是，在每一个构造器里面打印“father inits"或"son inits"，通过比较他们打印的先后顺序来判断，后来我又给构造器添加了一个带参数
的版本，发现还是会先打印"father inits",然后再执行当前的构造方法。

12.通过Animal这个接口，Cat和Dog都有了相同的标记，也即都是Animal，在他们内部又可通过重写给予接口中的方法不同的实现。

13.接口使得我们的程序设计得以提高抽象程度，上层设计得以高度抽象，这样下层设计及可以依赖于上层来实现，根据依赖倒置原则来说，这样可以更好地使得
下层依赖于上层，使得程序更好地开发，修改与维护。

14.
一、一个类只能继承一个抽象类，但是可以继承多个接口
二、抽象类中可以有data和非abstract method，但是interface中只有static final的data和abstract方法
三、抽象类表明类的从属关系，而interface有时候可以只是一个标签
四、interface中的方法在继承后必须全部实现，否则不能实例化变量